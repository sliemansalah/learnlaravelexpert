 cd "D:/laravel-projects/learnlaravel/learnlaravelexpert" && 

   php artisan tinker --execute="
   use App\Models\Teacher;
   use App\Models\Classroom;
   use App\Models\Student;
   use App\Models\Subject;
   use App\Models\Grade;

   echo '========================================' . PHP_EOL;
   echo '1. جلب أول معلم' . PHP_EOL;
   echo '========================================' . PHP_EOL;

   \$teacher = Teacher::first();
   echo 'الاسم: ' . \$teacher->name . PHP_EOL;
   echo 'التخصص: ' . \$teacher->specialization . PHP_EOL;
   echo 'الراتب: ' . \$teacher->salary . PHP_EOL;
   "
   Test Teacher model in Tinker

================================


 cd "D:/laravel-projects/learnlaravel/learnlaravelexpert" &&
  php -r "
   require 'vendor/autoload.php';
   \$app = require_once 'bootstrap/app.php';
   \$app->make('Illuminate\Contracts\Console\Kernel')->bootstrap();

   use App\Models\Teacher;
   use App\Models\Classroom;
   use App\Models\Student;
   use App\Models\Subject;
   use App\Models\Grade;

   echo '========================================' . PHP_EOL;
   echo '  1. جلب أول معلم' . PHP_EOL;
   echo '========================================' . PHP_EOL;

   \$teacher = Teacher::first();
   echo 'الاسم: ' . \$teacher->name . PHP_EOL;
   echo 'التخصص: ' . \$teacher->specialization . PHP_EOL;
   echo 'الراتب: ' . \$teacher->salary . PHP_EOL;
   echo PHP_EOL;
   "


   cd "D:/laravel-projects/learnlaravel/learnlaravelexpert" && 
   php artisan tinker --no-interaction << 'EOF'
   use App\Models\Teacher;
   $teacher = Teacher::first();
   echo "الاسم: " . $teacher->name . "\n";
   echo "التخصص: " . $teacher->specialization . "\n";
   exit;
   EOF



 cd "D:/laravel-projects/learnlaravel/learnlaravelexpert" && php artisan make:command TestRelationships



 cd "D:/laravel-projects/learnlaravel/learnlaravelexpert" && php artisan app:test-relationships


  ===========================================
        Testing School Management Relationships
===========================================


● ممتاز! تم تشغيل الاختبار بنجاح! 🎉

  ملخص ما تعلمناه من اختبار العلاقات:

  1. علاقة One-to-One (hasOne)

  // المعلم لديه فصل واحد
  $teacher->classroom->name;  // الصف 1 - أ

  2. علاقة belongsTo

  // الفصل ينتمي لمعلم
  $classroom->teacher->name;  // Miss Jacky Gutkowski
  $classroom->teacher->specialization;  // كيمياء

  3. علاقة One-to-Many (hasMany)

  // الفصل لديه طلاب كثير
  $classroom->students_count;  // 19 طالب
  $classroom->students()->take(5)->get();  // أول 5 طلاب

  4. علاقة One-to-Many (Teacher -> Subjects)

  // المعلم يدرس مواد
  $teacher->subjects;  // قائمة المواد
  $teacher->subjects_count;  // عدد المواد

  5. علاقة Many-to-Many

  // الطالب مسجل في مواد (والمادة لديها طلاب)
  $student->subjects()->distinct()->get();

  6. الوصول لبيانات Pivot

  // الدرجة، الفصل الدراسي، نوع الامتحان
  $subject->pivot->score;     // 89.09
  $subject->pivot->semester;  // الفصل الأول 2024
  $subject->pivot->exam_type; // midterm

  7. Eager Loading (التحميل المسبق)

  // يمنع مشكلة N+1 Query
  Classroom::with(['teacher', 'students'])->get();

  8. Helper Methods (الدوال المساعدة)

  $student->calculateGPA();      // 72.29
  $student->age();               // 9 years
  $subject->averageScore();      // 70.19
  $classroom->availableSeats();  // 11
  $grade->getLetterGrade();      // B

  9. Query Builder مع العلاقات

  // الطلاب الذين لديهم درجة 90 أو أكثر
  Student::whereHas('grades', fn($q) => $q->where('score', '>=', 90))->get();

  ---
  ما الخطوة التالية التي تريدها؟
  1. إنشاء Controllers و API Routes
  2. تعلم المزيد عن Query Scopes و Accessors/Mutators
  3. إنشاء Form Requests و Validation


