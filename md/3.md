
   cd "D:/laravel-projects/learnlaravel/learnlaravelexpert" &&
   
    php artisan make:model Teacher && php artisan
   make:model Classroom && php artisan make:model Student && php artisan make:model Subject && php artisan make:model
    Grade
   Create all models


إنشاء Models
==================
Teacher
Classroom
Student
Subject
Grade

======================================


cd "D:/laravel-projects/learnlaravel/learnlaravelexpert" && php artisan migrate:fresh)




 ---
  تم بنجاح! 🎉

  ملخص ما أنجزناه:

  ┌──────────────────────────────────────────────────────────────────────────┐
  │                    ✅ ما تم إنشاؤه                                       │
  ├──────────────────────────────────────────────────────────────────────────┤
  │                                                                           │
  │  📁 Migrations (ملفات إنشاء الجداول):                                    │
  │     ├── create_teachers_table                                            │
  │     ├── create_classrooms_table                                          │
  │     ├── create_students_table                                            │
  │     ├── create_subjects_table                                            │
  │     └── create_grades_table (pivot)                                      │
  │                                                                           │
  │  📁 Models (مع العلاقات):                                                │
  │     ├── Teacher    → hasOne(Classroom), hasMany(Subject)                 │
  │     ├── Classroom  → belongsTo(Teacher), hasMany(Student)                │
  │     ├── Student    → belongsTo(Classroom), belongsToMany(Subject)        │
  │     ├── Subject    → belongsTo(Teacher), belongsToMany(Student)          │
  │     └── Grade      → belongsTo(Student), belongsTo(Subject)              │
  │                                                                           │
  └──────────────────────────────────────────────────────────────────────────┘

  ---
  📝 شرح أنواع الأعمدة المستخدمة:

  | النوع                    | الاستخدام              | مثال          |
  |--------------------------|------------------------|---------------|
  | id()                     | مفتاح أساسي تلقائي     | -             |
  | string('name')           | نص قصير (255 حرف)      | الاسم         |
  | text('address')          | نص طويل                | العنوان       |
  | integer('age')           | رقم صحيح               | العمر         |
  | decimal('salary', 10, 2) | رقم عشري               | الراتب        |
  | date('birth_date')       | تاريخ                  | تاريخ الميلاد |
  | enum('status', [...])    | قائمة محددة            | الحالة        |
  | foreignId('teacher_id')  | مفتاح أجنبي            | ربط الجداول   |
  | timestamps()             | created_at, updated_at | -             |
  | softDeletes()            | deleted_at             | الحذف الناعم  |

  ---
  الخطوة التالية؟

  هل تريد أن ننتقل الآن إلى:
  1. إنشاء Seeders (بيانات تجريبية)
  2. إنشاء Controllers + API Routes
  3. اختبار العلاقات في Tinker

  ما رأيك؟
